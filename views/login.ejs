<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white flex justify-center items-center min-h-screen">

    <div class="w-full max-w-md bg-zinc-800 p-8 rounded-lg shadow-lg">
        <h3 class="text-2xl font-bold text-center mb-6">Login For More Video Download  </h3>
        
        <form action="/login" method="post" class="flex flex-col gap-4">
            <input class="px-4 py-3 rounded-md bg-zinc-700 border border-zinc-600 focus:border-blue-500" 
                type="email" placeholder="Enter your email" name="email">
            
            <input class="px-4 py-3 rounded-md bg-zinc-700 border border-zinc-600 focus:border-blue-500" 
                type="password" placeholder="Enter your password" name="password">
            
            <button class="bg-blue-600 hover:bg-blue-500 text-white px-4 py-3 rounded-md font-medium">Login</button>
        </form>
        <p class="text-center text-sm mt-4">
            Don't have an account? 
            <a href="/creatacount" class="text-blue-400 hover:underline">Sign up</a>
        </p>
    </div>
</body>
</html>
