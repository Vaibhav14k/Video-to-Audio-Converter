<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="">
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white  p-10 ">
        <div class="flex justify-end">
            <a class="px-3 py-2 bg-red-600 rounded-md  " href="logout">logout</a>
        </div>
        <div class="w-20 h-20 rounded-full bg-red-400 mb-5 "  >
            <img class="w-full h-full object-cover rounded-full overflow-hidden " src="/images/upload/<%= user.profilepic  %> " alt="">
        </div>
        <h3>hello <%= user.name %>
        </h3>
        <h5 class="mb-5">you can create a new post.</h5>
        <form method="post" action="/post">
            <textarea placeholder="Write your content for post "
                class="p-3 mb-3 w-1/3 bg-transparent border-2 resize-none " name="content" id=""></textarea> <br>
            <input class="px-3 w-28 py-2 bg-blue-500 rounded-md  " type="submit">
        </form>
        <div class="mt-20">
            <h3>Your post </h3>
            <div class="postcontainer  ">
                <div class="w-1/3 mb-10 border-2 p-4 rounded-md ">
                    <% user.post.forEach(function (post) { 
                        %>
                            <h4 class="text-blue-400">@ <%= user.username %></h4>
                            <p class="mt-2 text-white "> <%= post.content %> </p>
                            <div class="flex gap-4 mt-3">
                                <a class="text-blue-500" href="/like/<%=post._id %> ">like</a>
                                <a class="text-zinc-500" href="">edit</a>
                            </div>
                        <%
                    }) %>
                    
                </div>
            </div>
        </div>
    </div>
</body>

</html>